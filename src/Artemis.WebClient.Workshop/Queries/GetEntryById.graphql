query GetEntryById($id: UUID!) {
    entry(id: $id) {
        id
        author
        name
        summary
        entryType
        downloads
        createdAt
        categories {
            name
            icon
        }
    }
}